## From Illuminate Support package.

Remove all dependency code for `Illuminate` and use it alone in your project. 
It can provide many useful functions in your project, such as `Arr`, `Str`, `Optinal` and `Collection`.